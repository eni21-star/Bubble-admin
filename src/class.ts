import { NextFunction } from "express"

class DefaultClass { 

    async default(req: Request, res: Response, next: NextFunction): Promise<any>{
        
        try {

            
            
        } catch (error) {
            throw error
        }
    }
}